# Exemplo de arquivo de variáveis de ambiente
# Copie este arquivo para .env.local e preencha os valores corretos

# URL base da API do backend
NEXT_PUBLIC_API_URL=https://backend.casadamenina.com/api

# API Key para autenticação nas requisições
# Substitua ****** pela chave real fornecida pelo backend
NEXT_PUBLIC_API_KEY=******

# Ambiente de desenvolvimento (opcional)
NEXT_PUBLIC_ENVIRONMENT=development
